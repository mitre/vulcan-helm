apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vulcan-helm.fullname" . }}-test-connection"
  labels:
    {{- include "vulcan-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vulcan-helm.fullname" . }}:{{ .Values.vulcan.service.port }}']
  restartPolicy: Never
