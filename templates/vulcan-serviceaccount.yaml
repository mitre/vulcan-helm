{{- if .Values.vulcan.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vulcan-helm.serviceAccountName" . }}
  labels:
    {{- include "vulcan-helm.labels" . | nindent 4 }}
  {{- with .Values.vulcan.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
